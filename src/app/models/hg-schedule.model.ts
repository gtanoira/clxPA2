export interface HgScheduleModel {
  eventName: string;
  evento?: string;
  intervalValue: string;
  intervalField: string;
  intervalo?: string;
  lastExecuted?: string;
  status: string;
}
